import {
      IsString,
      IsNotEmpty,
      IsEmail,
      IsPhoneNumber,
      // IsNumber, 
      IsEnum,
      MinLength,
      MaxLength,
      // Min, 
      // Length, 
      // Max 
} from 'class-validator';
import { UUID } from 'crypto';

export class CreateUserDto {

      // website

}